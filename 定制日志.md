# Mattermost 品牌定制更改日志

## 📅 更改日期
2024年

## 🎯 定制目标
实现前端品牌定制，包括 Logo、网站名称、跳转链接、默认主题等配置，不依赖后端环境变量。

---

## 📁 新增文件

### 1. `webapp/channels/src/utils/brand_config.ts`
**功能**：品牌配置中心
- 集中管理所有品牌相关配置
- 包含网站名称、URL、品牌文本、主题、Logo路径、外部链接等
- 修改此文件即可更新所有品牌配置

**主要配置项**：
- `SITE_NAME` - 网站名称
- `SITE_URL` - 网站 URL
- `CUSTOM_BRAND_TEXT` - 自定义品牌文本
- `CUSTOM_DESCRIPTION_TEXT` - 自定义描述文本
- `DEFAULT_THEME` - 默认主题
- `LINKS` - 所有外部链接配置

---

### 2. `webapp/channels/src/utils/config_with_brand.ts`
**功能**：getConfig 包装器
- 包装原有的 `getConfig` 方法
- 自动合并前端品牌配置和后端配置
- 前端配置优先于后端配置
- 所有使用 `getConfig` 的地方自动获取品牌配置

**核心方法**：
- `getConfigWithBrand()` - 获取合并后的配置
- `getConfig` - 导出为 getConfig，方便替换
- `getOriginalConfigWithoutBrand()` - 获取原始后端配置

---

### 3. `webapp/channels/src/utils/constants_brand.ts`
**功能**：品牌链接常量
- 从 `brand_config.ts` 导入链接配置
- 重新导出所有链接常量（AboutLinks、CloudLinks、DocLinks等）
- 替换原有的 mattermost.com 链接为品牌链接

**导出的常量**：
- `AboutLinks` - 关于链接
- `CloudLinks` - 云服务链接
- `DocLinks` - 文档链接
- `DeveloperLinks` - 开发者链接
- `LicenseLinks` - 许可证链接
- `MattermostLink` - 主链接

---

### 4. `webapp/channels/src/utils/use_brand_config.ts`
**功能**：品牌配置 Hook
- 提供便捷的 React Hook 获取品牌配置
- 适用于函数组件
- 返回解构后的配置对象，使用更方便

**返回的配置项**：
- `siteName` - 网站名称
- `siteURL` - 网站 URL
- `customBrandText` - 自定义品牌文本
- `customDescriptionText` - 自定义描述文本
- `defaultTheme` - 默认主题
- `enableThemeSelection` - 是否启用主题选择
- `allowCustomThemes` - 是否允许自定义主题
- `config` - 完整配置对象

---

## 🔧 修改文件

### 1. `webapp/channels/src/utils/constants.tsx`
**修改内容**：
- 在文件末尾添加了品牌链接重新导出代码（目前为注释状态）
- 位置：第 2183 行之后

**启用方法**：
取消注释以下代码即可启用品牌链接：
```typescript
export {
    AboutLinks,
    CloudLinks,
    DocLinks,
    DeveloperLinks,
    LicenseLinks,
    MattermostLink,
} from './constants_brand';
```

---

## 🚀 使用方法

### 1. 配置品牌信息
编辑 `webapp/channels/src/utils/brand_config.ts`，修改配置值。

### 2. 替换 getConfig 导入
全局替换：
```
from 'mattermost-redux/selectors/entities/general'
→
from 'utils/config_with_brand'
```

### 3. 启用品牌链接（可选）
在 `constants.tsx` 中取消注释品牌链接重新导出代码。

### 4. 使用 Hook（可选）
在组件中使用 `useBrandConfig()` Hook 获取品牌配置。

---

## ✨ 功能特性

- ✅ **集中配置**：所有品牌配置集中在一个文件
- ✅ **自动覆盖**：前端配置自动覆盖后端配置
- ✅ **无需改页面**：通过包装 getConfig，所有使用它的地方自动生效
- ✅ **向后兼容**：原有代码继续工作
- ✅ **类型安全**：完整的 TypeScript 类型支持

---

## 📝 注意事项

1. **Logo 文件**：需要手动替换 `webapp/channels/src/images/` 目录下的 Logo 文件
2. **重新编译**：修改配置后需要重新编译前端
3. **链接常量**：如果启用品牌链接，需要注释掉 `constants.tsx` 中的原有链接定义

---

## 🔄 后续维护

- 更新品牌配置：修改 `brand_config.ts`
- 添加新配置项：在 `brand_config.ts` 中添加，在 `config_with_brand.ts` 中处理
- 更新链接：修改 `brand_config.ts` 中的 `LINKS` 配置

---
更改的文件

1、logo_dark_blue_svg webapp\channels\src\components\common\svg_images_components\ 更改svg
2、mattermost_logo webapp\channels\src\components\widgets\icons\ 更改svg

3、footer.tsx ->  GuDuu OS Inc.
4、webapp\channels\src\components\global_header\left_controls\product_menu\product_branding_team_edition\product_branding_free_edition.tsx -> 隐藏team 标签展示
5、webapp\channels\src\components\header_footer_route\header.tsx -> 隐藏team 标签展示
6、channels\src\i18n\en.json 更改文字
7、webapp\channels\src\components\widgets\menu\menu_items\menu_start_trial.tsx 隐藏TEAM EDITION